# Add .gitignore to a directory.

Add .gitignore to a directory.

## Usage

``` r
add_gitignore(path = getwd())
```

## Arguments

- path:

  The path to the directory where the git ignore file will be created.
